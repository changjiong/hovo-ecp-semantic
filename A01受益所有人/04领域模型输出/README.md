# 04 领域模型输出

本目录是 `domain-model` 在 A01 项目中的唯一输出边界。

- 根目录：当前正式交付 `input.json`、`model.yaml`、`review.md`、`coverage.md`、`output.json`。
- `process/review/`：模型任务、审阅和验证说明。
- `process/validation/`：本阶段结构/合同校验快照。
- `archive/`：旧模型、旧输入、旧审计和旧验证证据，只用于追溯。

后续修订继续使用本目录；中间记录不得回写到技能包目录，也不再创建公共的 `过程与评审记录` 目录。
