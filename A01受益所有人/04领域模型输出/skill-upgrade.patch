--- old/README.md
+++ new/README.md
@@ -1,30 +1,20 @@
 # domain-model
 
-形成与数据库、软件和平台无关的领域模型，解释业务对象及判断依据。Hovo 0.3.0，本地候选，资产合同2.0.0。
+把固定版本的领域知识整理成不依赖数据库、软件或平台的领域模型。当前为 Hovo 0.5.0 候选包，领域模型合同为 3.0.0；随包带有仅用于输入核对的领域知识 4.0.0 Schema 快照。
 
-> 使用 domain-model，依据这份已确认知识基线定义业务概念、属性、关系、时间和判断机理。
+PRODUCE 和 REVISE 显式选择 knowledge_basis：
 
-> 使用 domain-model，先审查这份已有模型。缺少知识确认可以作为发现，不要替我生成或批准新模型。
+- DRAFT 可基于未确认知识形成待审模型，但固定知识摘要，逐项映射开放上游问题，并禁止模型确认与正式交接。
+- CONFIRMED 需要真实外部 ConfirmationRecord，该记录必须精确绑定知识版本、摘要和本次问题范围。
 
-## 输入与交付
+review_mode 与知识依据独立：它管理会话或线下模型评审，不会把知识 PENDING 范围变成已确认范围。REVIEW 只审查已有模型。
 
-创建需要已确认的知识及确认依据；审查只需要已有模型；修订需要原模型、变更请求和已确认知识。技能负责整理结构化引用，不要求用户预先填写 JSON。人工和其他工具提供的成果均可使用，按当前合同核对，不要求先执行其他技能。
+PRODUCE 与 REVISE 交付 output.json、面向业务责任人的 review.md 和审计用 coverage.md。问题覆盖只可标为 MODELED、PARTIAL 或 DEFERRED；案例只可标为 EXPLAINED 或 BLOCKED。PARTIAL、DEFERRED 和 BLOCKED 保留问题与原因，不能宣称 PASS。
 
-创建或修订交付 output.json 与业务 review.md；审查交付定位明确的问题、建议、限制与审查报告。业务认可依赖真实确认记录。完整字段见 [输入](contracts/input.schema.json)、[输出](contracts/output.schema.json) 与 [合同说明](contracts/README.md)，完成条件见 [验收清单](references/acceptance.md)。
+在本包目录运行以下本地合同检查：
 
-## 独立使用与检查
+    python3 scripts/validate_contract.py --check-schemas
+    python3 scripts/validate_contract.py validate input /path/to/project/domain-model/input.json --project-root /path/to/project
+    python3 scripts/validate_contract.py validate output /path/to/project/domain-model/output.json --project-root /path/to/project
 
-完整目录可放入宿主支持的 skills 目录单独调用。本包自带资料、合同与工具，不需要其他技能或平台服务。Python 3.10+ 依赖见 [requirements.txt](requirements.txt)。在本技能目录运行：
-
-```bash
-python3 -m pip install -r requirements.txt
-python3 scripts/validate_contract.py --check-schemas
-python3 scripts/validate_contract.py validate input /path/to/project/domain-model/input.json --project-root /path/to/project
-python3 scripts/validate_contract.py validate output /path/to/project/domain-model/output.json --project-root /path/to/project
-```
-
-## 排查与证据
-
-悬空引用应修正具体来源或概念 ID；覆盖遗漏应补内容或明确缺口；案例预期变化应退回知识责任人确认。旧合同1.0.0不能作为2.0.0成果直接使用，重新编制时保留原始材料与变化依据，不能自动填入确认。
-
-本地检查证明结构与有限一致性，不证明来源真实、业务逻辑正确或专家认可。当前未执行真实模型生成评估、专家评审与宿主安装验证。旧 reports/standalone-checks.json 只证明0.2.0；本次记录见 [修订记录](reports/domain-revision.md)。资料快照见 [sources.json](references/sources.json)，权利与方法借鉴见 [来源说明](THIRD_PARTY_NOTICES.md)。
+本地检查核对结构、真实摘要、引用和覆盖约束；不证明来源真实性、业务正确性、人工确认、安装结果或运行状态。
--- old/SKILL.md
+++ new/SKILL.md
@@ -1,36 +1,37 @@
 ---
 name: domain-model
-description: 将已确认业务知识转成独立于数据库和平台的领域模型，或审查与修订已有模型。定义概念、角色、业务属性、关系、身份、时间、状态、证据和判断机理；不按物理字段造概念，不编制平台资产或数据映射。
+description: 将固定版本的领域知识转成独立于数据库和平台的领域模型，或审查、修订已有模型。区分待审草案与已确认依据，定义概念、属性、关系、时间、状态、证据和判断机理；不编制数据映射或平台资产。
 metadata:
   author: Hovo
-  version: "0.4.0"
+  version: "0.5.0"
 ---
 
 # 领域模型构建
 
-回答“业务世界由什么组成、如何变化、依据什么作出判断”。独立交付可供业务审查和后续需求确认的领域模型。
+回答“业务世界由什么组成、如何变化、依据什么作出判断”。交付可由业务责任人审阅、由后续阶段追溯的领域模型，不把表、字段、SQL、平台算子或运行配置写成业务定义。
 
 ## 入口与合同
 
-读取 [输入合同](contracts/input.schema.json)、[输出合同](contracts/output.schema.json)、[合同说明](contracts/README.md)、[验收清单](references/acceptance.md)。方法见 [六项检查](references/ontology-engineering-method.md) 与 [模式](references/modeling-patterns.md)。本目录资源自包含，不要求先运行其他技能。
+读取[输入合同](contracts/input.schema.json)、[输出合同](contracts/output.schema.json)、[合同说明](contracts/README.md)和[验收清单](references/acceptance.md)。本包随附领域知识 4.0.0 的输出与公共 Schema 快照，只用它核对上游成果；不读取或调用另一个技能目录。
 
-- PRODUCE：接收知识基线，先按[交互式评审](references/interactive-review.md)确定本次评审方式。`IN_SESSION` 在会话中完成责任人答复并绑定确认；`DEFERRED` 明确跳过本轮评审，交付待审草案，不伪造确认，也不把草案说成正式下游交接依据。
-- REVIEW：接收 subjects 中的已有模型即可开始，知识与确认记录为可选补充。缺依据形成审查限制，不阻止概念和结构审查。
-- REVISE：接收 subjects、change_request、已确认知识及确认依据，说明变化原因与影响；输出新版本，重新提交受影响范围确认。
+- PRODUCE 与 REVISE 必须选择 knowledge_basis，并独立选择 review_mode。review_mode 只说明本轮模型评审如何进行，不能替代上游知识确认。
+- DRAFT 固定知识 ArtifactRef 的版本、路径和摘要。将每个开放上游问题登记到 upstream_issue_bindings，映射为开放的 MODEL_GAP 和影响范围；问题覆盖写 MODELED、PARTIAL 或 DEFERRED，案例写 EXPLAINED 或 BLOCKED。模型 confirmation 必须为 PENDING，不能作正式交接或确认。
+- CONFIRMED 必须提供真实外部 ConfirmationRecord，精确绑定知识文件字节、责任人答复和本次 question_scope_ids。输出 evidence 也要保留该记录的绑定，便于独立核验。
+- REVIEW 只审查 subjects 中的既有模型，列出限制和 issues；不补造知识基线、确认记录或新模型。
 
-## 工作步骤
+## 建模与覆盖
 
-1. 判定模式、问题范围、已有成果和评审方式。创建或修订时核对知识身份、摘要与确认范围；审查时直接定位现有问题，不补造确认记录。
-2. 创建模型只使用业务知识、案例和规范模型；隔离物理表、字段、SQL、平台算子与运行配置。审查对象已有技术耦合时可定位并指出，不能把它作为新业务定义的依据。
-3. 为每个问题明确概念、业务属性、关系和机理落点。执行定义、粒度、身份、参与关系、时间计量、消费与反例六项检查。
-4. 区分实体、角色、时态事实、观察、证据、推导结果和任务。业务属性经“更换软件系统后是否仍成立”检查；不强制固定层数或为画图引入空壳节点。
-5. 将身份、时间、状态、事件、度量、证据和未知语义写成适用性及政策，绑定具体对象、属性或关系，列条件、行为、未知、冲突和禁止结果。
-6. 每项判断按业务步骤列明读取事实、引用规则、条件、操作、输出及缺证行为。展示可复核的业务方法，不索取私有思维过程，不发明平台 DSL。
-7. 固定 knowledge_ref 和 question_scope_ids，逐项核对问题覆盖与案例解释。沿用知识基线的预期及禁止结果；发现预期有误时提出变更请求，不能自行改写。
-8. 先交付可读的模型审查稿，集中提出重大设计分叉；`IN_SESSION` 时逐项收集真实答复、复述和批准范围后再封版，`DEFERRED` 时记录跳过原因、待审范围和恢复评审入口。再整理 output.json，运行授权范围内的本地引用检查。重大设计分叉以方案、依据、影响和具体例子提交责任人确认。
+1. 固定问题范围、知识依据、既有模型与变更请求。REVISE 先列受影响概念、关系、机理、问题和案例，产生新版本并重新评审受影响范围。
+2. 定义概念、角色、属性、关系、身份、时间、状态、事件、度量、证据和未知语义。每项写依据、正例和反例；业务属性经“更换软件系统后是否仍成立”检查。
+3. 每项判断机理列读取事实、引用规则、条件、业务操作、输出和缺证行为。不得编造平台 DSL、数据表或运行结论。
+4. question_coverage 逐项覆盖范围内知识问题。PARTIAL 与 DEFERRED 必须关联开放 MODEL_GAP 并说明原因；有开放缺口不得标记 MODELED。
+5. case_explanations 逐项解释范围内案例，逐字保留知识的 expected 与 forbidden。无法解释时写 BLOCKED 并关联其问题的开放模型缺口，不能用通过或 PASS 掩盖。
+6. PRODUCE 与 REVISE 同时交付 review.md 和 coverage.md：前者给业务人员阅读，后者是问题、案例、上游问题和模型落点的审计台账。两份文件须登记真实摘要；REVIEW 只交付审查意见。
 
-REVIEW 输出审查报告与 issues，不要求生成新概念、占位机理或已确认模型。缺业务依据时明确哪些结论只能作为建议。
+## 评审与完成边界
 
-## 完成边界
+按[交互式评审](references/interactive-review.md)处理会话和线下反馈。原始反馈绑定版本和范围；修订后，受影响范围的旧确认只保留为历史并重新评审。DRAFT 可以完成本轮模型草案，不等于业务认可、正式需求、发布或运行。
 
-结构与引用合格、业务问题和案例可解释、缺口明确，才报告可审查模型完成。`IN_SESSION` 的确认记录对应固定事项和版本；未确认不能声称业务认可。`DEFERRED` 草案可以结束当前技能回合，但只能作为待审模型，后续正式交接须先恢复评审。完成条件与任何平台的编译、发布或运行无关。
+结构和引用检查只能证明合同形状及有限一致性。它不证明来源真实、业务逻辑正确、责任人身份、平台编译、发布、运行或任何受益所有人结论。
+
+创建和修订必须明确非空问题范围，且属于知识问题目录与知识声明范围；声明范围本身不是批准。DRAFT和CONFIRMED都逐项处置上游开放事项，范围外事项可注明不适用及原因，不能遗漏已影响本次问题的缺口。每题覆盖表须列全实际影响该题的开放模型缺口。知识的正式确认记录同时绑定知识output、review.md、coverage.md及真实答复；已REJECTED或STALE的基线不能借旧批准绕过。
--- old/agents/interface.yaml
+++ new/agents/interface.yaml
@@ -1,13 +1,13 @@
 name: domain-model
-version: "0.3.0"
+version: "0.5.0"
 interface:
   display_name: "领域模型构建"
-  short_description: "建立独立于物理数据与平台的规范领域模型。"
-  default_prompt: "使用 $domain-model，只依据已确认的业务知识与案例定义概念、业务属性、关系、身份、时间和判断机理，完成模型审查并提交确认。"
+  short_description: "以待审或已确认知识建立独立领域模型。"
+  default_prompt: "使用 $domain-model，选择 DRAFT 或 CONFIRMED 知识依据，定义概念、业务属性、关系、身份、时间和判断机理；逐项说明问题、案例与上游未决项。"
 compatibility:
   adapter_targets: [openai, claude, generic, agent-skills-compatible]
   canonical_format: "Agent Skills Markdown with JSON Schema"
-  dependencies: "本目录固定版本资料与 requirements.txt；无其他技能依赖"
+  dependencies: "本目录固定模型合同及领域知识 4.0.0 Schema 快照；无其他技能依赖"
   host_installation: "NOT_EXECUTED"
   context_isolation: "宿主能力需独立验证，说明文件不提供沙箱"
   permissions: "创建、审查、修订按任务范围；不替责任人确认业务成果"
--- old/contracts/README.md
+++ new/contracts/README.md
@@ -1,35 +1,34 @@
-# 平台无关领域资产合同 2.0.0
+# 平台无关领域模型合同 3.0.0
 
-本技能的输入、输出和公共对象均随包提供；公共对象只管理资产身份、版本、来源、证据、案例、问题和审查状态。本包不加载其他技能目录。2.0.0 替代原1.0.0领域合同，不接受旧版作为新版成果，不自动迁移或填造缺项。
+本包的公共对象、模型输入输出 Schema、领域知识 4.0.0 输出 Schema 和其公共 Schema 都随包提供。领域知识快照只用于核对输入，不要求安装或调用 domain-knowledge，也不接受 2.0.0 或其他旧知识合同作为 3.0.0 的上游资产。
 
-## 使用与模式
+## 输入与依据状态
 
-用户可提供文档、业务说明或其他工具成果。由技能形成机器可检查的交接文件，保留原文件、定位与含义；JSON 不是用户访谈的前置要求。
+PRODUCE 和 REVISE 继续使用 request_id、mode、scope、knowledge、question_scope_ids 与 review_mode，并新增必选 knowledge_basis。REVIEW 只需要 subjects。
 
-PRODUCE 创建模型草案；REVIEW 只审查 subjects 指定的对象；REVISE 依据明确变更请求更新指定成果。创建和修订必须声明 `review_mode`：`IN_SESSION` 要求知识确认记录，并在当前会话完成范围内的真实答复；`DEFERRED` 允许没有确认记录，但产物只能是待审草案，不能声称正式下游交接。REVIEW 无需已有业务确认，输出 content.subjects、assessment、limitations 与顶层 issues，不输出 confirmation，也不伪造一份完整基线来容纳审查发现。
+- DRAFT：knowledge 必须是符合 4.0.0 的非 REVIEW 知识成果，且 ArtifactRef 的路径、版本和 SHA-256 摘要可读取并匹配。不得附带 knowledge_confirmation，也不得把知识 confirmation 的 PENDING scope 视为已确认范围。
+- CONFIRMED：除上述精确知识引用外，必须提供 knowledge_confirmation。它是指向外部 ConfirmationRecord 的 EvidenceRef；记录必须 APPROVED、同时绑定该知识 ArtifactRef 的完整身份和字节摘要，并覆盖 question_scope_ids。
+- review_mode 的 IN_SESSION 或 DEFERRED 仅描述模型评审方式。它不改变 knowledge_basis，也不产生确认。
 
-## 文件、身份与状态
+## 输出与审计
 
-output.json 维护结构化成果，review.md 面向业务责任人呈现同一内容。稳定 artifact_id 与内部 ID 用于追踪，content_version 随变化，contract_version 固定本合同版本。引用使用完整 ArtifactRef，path 相对任务项目根目录，digest 是文件原始字节的 SHA-256；禁止路径越界、符号链接和凭据。
+所有非 REVIEW 输出的 content 保留 concepts、attributes、relationships、semantics、mechanisms、question_coverage、case_explanations、decisions、knowledge_ref 和 question_scope_ids，并新增：
 
-states 只记录 structure_checked 与 business_reviewed。前者证明实际结构与引用检查，后者记录业务内容审查。确认单独使用 confirmation 和外部 ConfirmationRecord，不继承任何平台编译、发布或运行状态。
+- knowledge_basis：DRAFT 或 CONFIRMED，与输入选择一致。
+- upstream_issue_bindings：每项列 knowledge_issue_id、对应开放 MODEL_GAP、处理方式和理由。DRAFT 必须逐项覆盖上游所有 OPEN issue；通用语义未完整也必须登记为模型限制。
+- question_coverage.status：MODELED、PARTIAL 或 DEFERRED，并写原因。PARTIAL 和 DEFERRED 关联开放 MODEL_GAP；存在该问题的开放缺口时不得写 MODELED。
+- case_explanations.status：EXPLAINED 或 BLOCKED。BLOCKED 保留原案例 expected 和 forbidden，并由关联问题的 PARTIAL 或 DEFERRED 说明。
 
-未执行写 NOT_EXECUTED；已执行的 PASS、FAIL、ERROR 必须有实际证据。不适用必须说明理由。确认对象的摘要不能写入它自身；固定草案后计算摘要，外部确认记录引用该文件和真实答复。确认不会修改已经签认的文件，后续使用同时携带该成果与其独立确认记录。
+knowledge_ref 也必须出现在 input_refs。CONFIRMED 输出的 evidence 中必须有一个真实外部 ConfirmationRecord 绑定知识文件和模型范围，供输出单独检查。DRAFT 输出的 confirmation.status 必须为 PENDING；因此没有正式下游交接或业务确认。
 
-## 引用与确认
+PRODUCE 和 REVISE 必须登记 review.md 与 coverage.md，且都使用当前 content_version 和真实摘要。review.md 面向业务责任人呈现范围、模型、判断、案例与待确认事项；coverage.md 是问题、案例、上游 issue、模型 issue 和引用的审计表。REVIEW 只登记审查意见，不生成待审模型。
 
-SourceRef 定位来源主体、时间、版本和原文位置。Statement.source_ids 指向来源，Term/Rule.statement_ids 指向陈述，Case.question_ids 指向业务问题。模型 basis_ids 指向所绑定知识中的陈述、术语或规则；关系端点、属性归属和语义政策必须指向已声明的模型要素。
+## 线下与会话评审
 
-模型 content.knowledge_ref 必须同时登记在 input_refs，固定准确知识版本。question_scope_ids 是本模型承接的知识问题范围，必须与模型 confirmation.scope_ids 一致；知识与模型的新 ID 使用明确前缀避免冲突。案例预期与禁止结果逐字保留，业务解释另写，不用重述来隐藏规则变化。
+原始反馈保持为独立 ArtifactRef，写明固定的版本、摘要、问题范围、答复者、角色、时间和逐项意见。修订必须新建版本，列受影响问题、案例、概念和机制；受影响的旧确认是历史证据，不覆盖新文件。业务人员审阅 review.md 并答复业务事项，不编辑 JSON 或内部 ID。
 
-`IN_SESSION` 的确认记录须包含真实责任人、角色、时间、scope_ids、完整 subjects 和答复文件。知识确认范围使用业务问题 ID；模型创建范围必须被知识确认范围覆盖。`DEFERRED` 的 confirmation 必须保持 `PENDING` 并说明未审范围及恢复入口。具体陈述或规则的 review_status 不抹去其 origin 或争议记录；声称已确认时还需对应 evidence 引用，审查者核对答复事项与原文是否匹配。作者或脚本不能替代责任人。
+## 检查边界
 
-issues 记录问题、影响 ID、建议、责任和解决前行为。REVIEW 的 affects 可以指向被审资产 ID，并用 locator 指明原文位置。业务缺口与冲突不因检查工具返回 PASS 而消失。trace 两端必须落在本次资产、来源、模型或所引用知识的有效标识上，禁止用自由文本冒充引用。
+本地校验检查 JSON Schema、字节摘要、引用、范围、问题和案例覆盖、确认记录的结构与精确绑定。它不证明来源真实性、正文语义正确、确认人身份、业务验收、平台发布、运行或实际控制人结论。
 
-## 业务完成与校验范围
-
-关键问题审查正例、反例、边界与缺证，确实不适用写理由；未覆盖写明确缺口。阶段内校验能拒绝悬空引用、错误类型、重复或遗漏覆盖、基数冲突、部分循环及被改写的案例预期。它不能证明来源真实性、业务逻辑正确性、审查者身份或宿主上下文隔离。
-
-本地入口为 scripts/validate_contract.py --check-schemas 和 validate input|output FILE --project-root ROOT。Registry 按本包 Schema 的 $id 离线解析，hovo.local 只是命名空间；不访问网络、不读取其他技能合同。
-
-修改已确认业务内容时，先列影响，再产生新版本并重新确认受影响事项。旧证据保留为历史，不用于变化后的字节。公共合同的固定快照与工具摘要见 [sources.json](../references/sources.json)。
+创建和修订必须明确非空问题范围，且属于知识问题目录与知识声明范围；声明范围本身不是批准。DRAFT和CONFIRMED都逐项处置上游开放事项，范围外事项可注明不适用及原因，不能遗漏已影响本次问题的缺口。每题覆盖表须列全实际影响该题的开放模型缺口。知识的正式确认记录同时绑定知识output、review.md、coverage.md及真实答复；已REJECTED或STALE的基线不能借旧批准绕过。
--- old/contracts/common.schema.json
+++ new/contracts/common.schema.json
@@ -1,7 +1,7 @@
 {
   "$schema": "https://json-schema.org/draft/2020-12/schema",
-  "$id": "https://hovo.local/semantic-engineering/2/common.schema.json",
-  "title": "Platform-independent Domain Asset Contract 2.0.0",
+  "$id": "https://hovo.local/semantic-engineering/3/common.schema.json",
+  "title": "Platform-independent Domain Asset Contract 3.0.0",
   "$defs": {
     "Text": {
       "type": "string",
@@ -524,7 +524,7 @@
           "$ref": "#/$defs/Text"
         },
         "contract_version": {
-          "const": "2.0.0"
+          "const": "3.0.0"
         },
         "stage": {
           "$ref": "#/$defs/Text"
--- old/contracts/imports/README.md
+++ new/contracts/imports/README.md
@@ -1,5 +1,5 @@
-# 输入资产合同
+# 随包上游合同快照
 
-本目录只携带本技能核对输入所需的资产 Schema，不包含其他技能入口、生产流程或执行代码。文件描述允许的输入形状，不要求安装或运行同名技能。
+domain-knowledge.schema.json 与 domain-knowledge-common.schema.json 是领域知识 4.0.0 输出合同及其公共 Schema 的固定副本。它们只用于校验本技能接收的上游知识文件，不能替代领域知识技能、来源审查、业务确认或平台运行。
 
-输入可以由人工、其他工具或其他技能提供。已有成果须按本技能合同整理引用与确认记录，保留原始来源；不得为了适配合同而伪造来源、批准或改写业务含义。
+本包拒绝旧知识合同作为 3.0.0 模型合同的输入，不提供兼容层、迁移或本地回退。更新上游合同必须同时更新两个 Schema、输入输出约束、校验器和本文说明。
--- old/contracts/imports/domain-knowledge-common.schema.json
+++ new/contracts/imports/domain-knowledge-common.schema.json
@@ -0,0 +1,648 @@
+{
+  "$schema": "https://json-schema.org/draft/2020-12/schema",
+  "$id": "https://hovo.local/semantic-engineering/4/common.schema.json",
+  "title": "Platform-independent Domain Asset Contract 4.0.0",
+  "$defs": {
+    "Text": {
+      "type": "string",
+      "minLength": 1
+    },
+    "Id": {
+      "type": "string",
+      "pattern": "^[A-Za-z][A-Za-z0-9._:-]*$"
+    },
+    "Digest": {
+      "type": "string",
+      "pattern": "^sha256:[a-f0-9]{64}$"
+    },
+    "Path": {
+      "type": "string",
+      "minLength": 1,
+      "pattern": "^(?!/)(?!.*\\\\)(?!.*(?:^|/)\\.\\.(?:/|$)).+$"
+    },
+    "ArtifactRef": {
+      "type": "object",
+      "additionalProperties": false,
+      "required": [
+        "artifact_id",
+        "content_version",
+        "contract_version",
+        "path",
+        "digest"
+      ],
+      "properties": {
+        "artifact_id": {
+          "$ref": "#/$defs/Id"
+        },
+        "content_version": {
+          "$ref": "#/$defs/Text"
+        },
+        "contract_version": {
+          "$ref": "#/$defs/Text"
+        },
+        "path": {
+          "$ref": "#/$defs/Path"
+        },
+        "digest": {
+          "$ref": "#/$defs/Digest"
+        }
+      }
+    },
+    "Refs": {
+      "type": "array",
+      "items": {
+        "$ref": "#/$defs/ArtifactRef"
+      },
+      "uniqueItems": true
+    },
+    "Ids": {
+      "type": "array",
+      "items": {
+        "$ref": "#/$defs/Id"
+      },
+      "uniqueItems": true
+    },
+    "SourceRef": {
+      "type": "object",
+      "additionalProperties": false,
+      "required": [
+        "source_id",
+        "artifact",
+        "issuer",
+        "date",
+        "locator",
+        "authority",
+        "source_role"
+      ],
+      "properties": {
+        "source_id": {
+          "$ref": "#/$defs/Id"
+        },
+        "artifact": {
+          "$ref": "#/$defs/ArtifactRef"
+        },
+        "issuer": {
+          "$ref": "#/$defs/Text"
+        },
+        "date": {
+          "$ref": "#/$defs/Text"
+        },
+        "locator": {
+          "$ref": "#/$defs/Text"
+        },
+        "authority": {
+          "enum": [
+            "PRIMARY",
+            "EXPERT",
+            "SECONDARY",
+            "SYSTEM_DESCRIPTION",
+            "UNKNOWN"
+          ]
+        },
+        "source_role": {
+          "enum": [
+            "NORMATIVE_RULE",
+            "PROCEDURAL_GUIDANCE",
+            "SYSTEM_INTERFACE",
+            "EXPERT_EXPLANATION",
+            "SECONDARY_CONTEXT",
+            "UNKNOWN"
+          ]
+        }
+      }
+    },
+    "EvidenceRef": {
+      "type": "object",
+      "additionalProperties": false,
+      "required": [
+        "evidence_id",
+        "record",
+        "locator",
+        "claim",
+        "subject_refs"
+      ],
+      "properties": {
+        "evidence_id": {
+          "$ref": "#/$defs/Id"
+        },
+        "record": {
+          "$ref": "#/$defs/ArtifactRef"
+        },
+        "locator": {
+          "$ref": "#/$defs/Text"
+        },
+        "claim": {
+          "$ref": "#/$defs/Text"
+        },
+        "subject_refs": {
+          "$ref": "#/$defs/Refs",
+          "minItems": 1
+        }
+      }
+    },
+    "Check": {
+      "type": "object",
+      "additionalProperties": false,
+      "required": [
+        "status",
+        "evidence_ids",
+        "reason"
+      ],
+      "properties": {
+        "status": {
+          "enum": [
+            "NOT_EXECUTED",
+            "PASS",
+            "FAIL",
+            "ERROR",
+            "NOT_APPLICABLE"
+          ]
+        },
+        "evidence_ids": {
+          "$ref": "#/$defs/Ids"
+        },
+        "reason": {
+          "$ref": "#/$defs/Text"
+        }
+      },
+      "allOf": [
+        {
+          "if": {
+            "properties": {
+              "status": {
+                "enum": [
+                  "PASS",
+                  "FAIL",
+                  "ERROR"
+                ]
+              }
+            }
+          },
+          "then": {
+            "properties": {
+              "evidence_ids": {
+                "minItems": 1
+              }
+            }
+          }
+        }
+      ]
+    },
+    "States": {
+      "type": "object",
+      "additionalProperties": false,
+      "required": [
+        "structure_checked",
+        "business_reviewed"
+      ],
+      "properties": {
+        "structure_checked": {
+          "$ref": "#/$defs/Check"
+        },
+        "business_reviewed": {
+          "$ref": "#/$defs/Check"
+        }
+      }
+    },
+    "ConfirmationRecord": {
+      "type": "object",
+      "additionalProperties": false,
+      "required": [
+        "confirmation_id",
+        "status",
+        "person",
+        "role",
+        "recorded_at",
+        "scope_ids",
+        "subjects",
+        "response"
+      ],
+      "properties": {
+        "confirmation_id": {
+          "$ref": "#/$defs/Id"
+        },
+        "status": {
+          "enum": [
+            "APPROVED",
+            "REJECTED"
+          ]
+        },
+        "person": {
+          "$ref": "#/$defs/Text"
+        },
+        "role": {
+          "$ref": "#/$defs/Text"
+        },
+        "recorded_at": {
+          "type": "string",
+          "format": "date-time"
+        },
+        "scope_ids": {
+          "$ref": "#/$defs/Ids",
+          "minItems": 1
+        },
+        "subjects": {
+          "$ref": "#/$defs/Refs",
+          "minItems": 1
+        },
+        "response": {
+          "$ref": "#/$defs/ArtifactRef"
+        }
+      }
+    },
+    "Confirmation": {
+      "type": "object",
+      "additionalProperties": false,
+      "required": [
+        "status",
+        "scope_ids",
+        "evidence_ids"
+      ],
+      "properties": {
+        "status": {
+          "enum": [
+            "PENDING",
+            "CONFIRMED",
+            "REJECTED",
+            "STALE"
+          ]
+        },
+        "scope_ids": {
+          "$ref": "#/$defs/Ids",
+          "minItems": 1
+        },
+        "evidence_ids": {
+          "$ref": "#/$defs/Ids"
+        }
+      },
+      "allOf": [
+        {
+          "if": {
+            "properties": {
+              "status": {
+                "enum": [
+                  "CONFIRMED",
+                  "REJECTED"
+                ]
+              }
+            }
+          },
+          "then": {
+            "properties": {
+              "evidence_ids": {
+                "minItems": 1
+              }
+            }
+          }
+        }
+      ]
+    },
+    "Issue": {
+      "type": "object",
+      "additionalProperties": false,
+      "required": [
+        "id",
+        "kind",
+        "statement",
+        "affects",
+        "owner",
+        "recommendation",
+        "until_resolved",
+        "status"
+      ],
+      "properties": {
+        "id": {
+          "$ref": "#/$defs/Id"
+        },
+        "kind": {
+          "enum": [
+            "CONFLICT",
+            "KNOWLEDGE_GAP",
+            "MODEL_GAP",
+            "CHANGE_REQUEST"
+          ]
+        },
+        "statement": {
+          "$ref": "#/$defs/Text"
+        },
+        "affects": {
+          "$ref": "#/$defs/Ids",
+          "minItems": 1
+        },
+        "owner": {
+          "$ref": "#/$defs/Text"
+        },
+        "recommendation": {
+          "$ref": "#/$defs/Text"
+        },
+        "until_resolved": {
+          "$ref": "#/$defs/Text"
+        },
+        "status": {
+          "enum": [
+            "OPEN",
+            "RESOLVED",
+            "REJECTED"
+          ]
+        },
+        "resolution_evidence_ids": {
+          "$ref": "#/$defs/Ids"
+        },
+        "locator": {
+          "$ref": "#/$defs/Text"
+        },
+        "severity": {
+          "enum": [
+            "HIGH",
+            "MEDIUM",
+            "LOW"
+          ]
+        }
+      },
+      "allOf": [
+        {
+          "if": {
+            "properties": {
+              "status": {
+                "enum": [
+                  "RESOLVED",
+                  "REJECTED"
+                ]
+              }
+            }
+          },
+          "then": {
+            "required": [
+              "resolution_evidence_ids"
+            ],
+            "properties": {
+              "resolution_evidence_ids": {
+                "minItems": 1
+              }
+            }
+          }
+        }
+      ]
+    },
+    "Case": {
+      "type": "object",
+      "additionalProperties": false,
+      "required": [
+        "id",
+        "question_ids",
+        "kind",
+        "input_facts",
+        "expected",
+        "forbidden",
+        "source_ids",
+        "synthetic"
+      ],
+      "properties": {
+        "id": {
+          "$ref": "#/$defs/Id"
+        },
+        "question_ids": {
+          "$ref": "#/$defs/Ids",
+          "minItems": 1
+        },
+        "kind": {
+          "enum": [
+            "POSITIVE",
+            "NEGATIVE",
+            "BOUNDARY",
+            "MISSING_EVIDENCE"
+          ]
+        },
+        "input_facts": {
+          "type": "array",
+          "minItems": 1,
+          "items": {
+            "$ref": "#/$defs/Text"
+          }
+        },
+        "expected": {
+          "$ref": "#/$defs/Text"
+        },
+        "forbidden": {
+          "$ref": "#/$defs/Text"
+        },
+        "source_ids": {
+          "$ref": "#/$defs/Ids"
+        },
+        "synthetic": {
+          "type": "boolean"
+        },
+        "reasoning": {
+          "$ref": "#/$defs/Text"
+        }
+      },
+      "allOf": [
+        {
+          "if": {
+            "properties": {
+              "synthetic": {
+                "const": false
+              }
+            }
+          },
+          "then": {
+            "properties": {
+              "source_ids": {
+                "minItems": 1
+              }
+            }
+          }
+        }
+      ]
+    },
+    "Trace": {
+      "type": "object",
+      "additionalProperties": false,
+      "required": [
+        "from_id",
+        "to_id",
+        "relation",
+        "basis"
+      ],
+      "properties": {
+        "from_id": {
+          "$ref": "#/$defs/Id"
+        },
+        "to_id": {
+          "$ref": "#/$defs/Id"
+        },
+        "relation": {
+          "enum": [
+            "SUPPORTS",
+            "FORMALIZES",
+            "EXERCISES",
+            "DEPENDS_ON",
+            "CONTRADICTS"
+          ]
+        },
+        "basis": {
+          "$ref": "#/$defs/Text"
+        }
+      }
+    },
+    "Input": {
+      "type": "object",
+      "required": [
+        "request_id",
+        "contract_version",
+        "mode",
+        "scope"
+      ],
+      "properties": {
+        "request_id": {
+          "$ref": "#/$defs/Id"
+        },
+        "contract_version": {
+          "const": "4.0.0"
+        },
+        "mode": {
+          "enum": [
+            "PRODUCE",
+            "REVIEW",
+            "REVISE"
+          ]
+        },
+        "scope": {
+          "$ref": "#/$defs/Text"
+        },
+        "evidence": {
+          "type": "array",
+          "items": {
+            "$ref": "#/$defs/EvidenceRef"
+          }
+        }
+      }
+    },
+    "Output": {
+      "type": "object",
+      "required": [
+        "artifact_id",
+        "content_version",
+        "contract_version",
+        "stage",
+        "input_refs",
+        "files",
+        "evidence",
+        "states",
+        "issues",
+        "trace",
+        "content",
+        "mode"
+      ],
+      "properties": {
+        "artifact_id": {
+          "$ref": "#/$defs/Id"
+        },
+        "content_version": {
+          "$ref": "#/$defs/Text"
+        },
+        "contract_version": {
+          "const": "4.0.0"
+        },
+        "stage": {
+          "$ref": "#/$defs/Text"
+        },
+        "input_refs": {
+          "$ref": "#/$defs/Refs",
+          "minItems": 1
+        },
+        "files": {
+          "$ref": "#/$defs/Refs",
+          "minItems": 1
+        },
+        "evidence": {
+          "type": "array",
+          "items": {
+            "$ref": "#/$defs/EvidenceRef"
+          }
+        },
+        "states": {
+          "$ref": "#/$defs/States"
+        },
+        "confirmation": {
+          "$ref": "#/$defs/Confirmation"
+        },
+        "issues": {
+          "type": "array",
+          "items": {
+            "$ref": "#/$defs/Issue"
+          }
+        },
+        "trace": {
+          "type": "array",
+          "items": {
+            "$ref": "#/$defs/Trace"
+          },
+          "minItems": 1
+        },
+        "content": {
+          "type": "object"
+        },
+        "mode": {
+          "enum": [
+            "PRODUCE",
+            "REVIEW",
+            "REVISE"
+          ]
+        }
+      },
+      "allOf": [
+        {
+          "if": {
+            "properties": {
+              "mode": {
+                "const": "REVIEW"
+              }
+            }
+          },
+          "then": {
+            "properties": {
+              "content": {
+                "$ref": "#/$defs/ReviewContent"
+              }
+            },
+            "not": {
+              "required": [
+                "confirmation"
+              ]
+            }
+          },
+          "else": {
+            "required": [
+              "confirmation"
+            ]
+          }
+        }
+      ]
+    },
+    "ReviewContent": {
+      "type": "object",
+      "additionalProperties": false,
+      "required": [
+        "subjects",
+        "assessment",
+        "limitations"
+      ],
+      "properties": {
+        "subjects": {
+          "$ref": "#/$defs/Refs",
+          "minItems": 1
+        },
+        "assessment": {
+          "$ref": "#/$defs/Text"
+        },
+        "limitations": {
+          "type": "array",
+          "items": {
+            "$ref": "#/$defs/Text"
+          }
+        }
+      }
+    }
+  }
+}
--- old/contracts/imports/domain-knowledge.schema.json
+++ new/contracts/imports/domain-knowledge.schema.json
@@ -1,6 +1,6 @@
 {
   "$schema": "https://json-schema.org/draft/2020-12/schema",
-  "$id": "https://hovo.local/semantic-engineering/2/domain-knowledge/output.schema.json",
+  "$id": "https://hovo.local/semantic-engineering/4/domain-knowledge/output.schema.json",
   "allOf": [
     {
       "$ref": "../common.schema.json#/$defs/Output"
@@ -33,7 +33,9 @@
               "questions",
               "rules",
               "cases",
-              "case_coverage"
+              "case_coverage",
+              "provision_coverage",
+              "question_discovery"
             ],
             "properties": {
               "scope": {
@@ -85,6 +87,16 @@
                 "items": {
                   "$ref": "#/$defs/CaseCoverage"
                 }
+              },
+              "provision_coverage": {
+                "type": "array",
+                "minItems": 1,
+                "items": {
+                  "$ref": "#/$defs/ProvisionCoverage"
+                }
+              },
+              "question_discovery": {
+                "$ref": "#/$defs/QuestionDiscovery"
               }
             }
           }
@@ -106,8 +118,10 @@
         "id",
         "text",
         "source_ids",
+        "source_unit_ids",
         "basis",
         "origin",
+        "meaning_kind",
         "review_status",
         "dispute_status",
         "conflict_ids"
@@ -120,8 +134,10 @@
           "$ref": "../common.schema.json#/$defs/Text"
         },
         "source_ids": {
-          "$ref": "../common.schema.json#/$defs/Ids",
-          "minItems": 1
+          "$ref": "../common.schema.json#/$defs/Ids"
+        },
+        "source_unit_ids": {
+          "$ref": "../common.schema.json#/$defs/Ids"
         },
         "basis": {
           "$ref": "../common.schema.json#/$defs/Text"
@@ -136,6 +152,21 @@
             "PROPOSED"
           ]
         },
+        "meaning_kind": {
+          "enum": [
+            "SOURCE_EXCERPT",
+            "DEFINITION",
+            "OBLIGATION",
+            "PERMISSION",
+            "PROHIBITION",
+            "CRITERION",
+            "PROCEDURE",
+            "EVIDENCE",
+            "TIME",
+            "EXCEPTION",
+            "CONTEXT"
+          ]
+        },
         "review_status": {
           "enum": [
             "PENDING",
@@ -161,6 +192,21 @@
         {
           "if": {
             "properties": {
+              "origin": {
+                "const": "SOURCE_STATED"
+              }
+            }
+          },
+          "then": {
+            "properties": {
+              "source_ids": {"minItems": 1},
+              "source_unit_ids": {"minItems": 1}
+            }
+          }
+        },
+        {
+          "if": {
+            "properties": {
               "review_status": {
                 "enum": [
                   "CONFIRMED",
@@ -260,6 +306,12 @@
         },
         "distinctions": {
           "$ref": "../common.schema.json#/$defs/Text"
+        },
+        "example": {
+          "$ref": "../common.schema.json#/$defs/Text"
+        },
+        "counterexample": {
+          "$ref": "../common.schema.json#/$defs/Text"
         }
       }
     },
@@ -269,6 +321,7 @@
       "required": [
         "id",
         "question",
+        "topic",
         "consumer",
         "decision_use",
         "unknown_policy"
@@ -278,6 +331,15 @@
           "$ref": "../common.schema.json#/$defs/Id"
         },
         "question": {
+          "$ref": "../common.schema.json#/$defs/Text"
+        },
+        "topic": {
+          "$ref": "../common.schema.json#/$defs/Text"
+        },
+        "parent_question_id": {
+          "$ref": "../common.schema.json#/$defs/Id"
+        },
+        "explanation": {
           "$ref": "../common.schema.json#/$defs/Text"
         },
         "consumer": {
@@ -426,6 +488,135 @@
         }
       ]
     },
+    "ProvisionCoverage": {
+      "type": "object",
+      "additionalProperties": false,
+      "required": [
+        "id",
+        "source_unit_id",
+        "status",
+        "meaning_status",
+        "meaning_note",
+        "business_topic",
+        "business_meaning",
+        "applicability",
+        "statement_ids",
+        "term_ids",
+        "question_ids",
+        "rule_ids",
+        "case_ids",
+        "issue_ids",
+        "reason"
+      ],
+      "properties": {
+        "id": {"$ref": "../common.schema.json#/$defs/Id"},
+        "source_unit_id": {"$ref": "../common.schema.json#/$defs/Id"},
+        "status": {"enum": ["COVERED", "PARTIAL", "OUT_OF_SCOPE", "UNREADABLE"]},
+        "meaning_status": {"enum": ["REVIEWED", "PARTIAL", "NOT_REVIEWED", "NOT_APPLICABLE"]},
+        "meaning_note": {"$ref": "../common.schema.json#/$defs/Text"},
+        "business_topic": {"$ref": "../common.schema.json#/$defs/Text"},
+        "business_meaning": {"$ref": "../common.schema.json#/$defs/Text"},
+        "applicability": {"$ref": "../common.schema.json#/$defs/Text"},
+        "statement_ids": {"$ref": "../common.schema.json#/$defs/Ids"},
+        "term_ids": {"$ref": "../common.schema.json#/$defs/Ids"},
+        "question_ids": {"$ref": "../common.schema.json#/$defs/Ids"},
+        "rule_ids": {"$ref": "../common.schema.json#/$defs/Ids"},
+        "case_ids": {"$ref": "../common.schema.json#/$defs/Ids"},
+        "issue_ids": {"$ref": "../common.schema.json#/$defs/Ids"},
+        "reason": {"$ref": "../common.schema.json#/$defs/Text"}
+      }
+    },
+    "QuestionDiscovery": {
+      "type": "object",
+      "additionalProperties": false,
+      "required": [
+        "scope_question_ids",
+        "candidates",
+        "process_checks"
+      ],
+      "properties": {
+        "scope_question_ids": {
+          "$ref": "../common.schema.json#/$defs/Ids",
+          "minItems": 1
+        },
+        "candidates": {
+          "type": "array",
+          "items": {
+            "$ref": "#/$defs/QuestionCandidate"
+          }
+        },
+        "process_checks": {
+          "type": "array",
+          "items": {
+            "$ref": "#/$defs/ProcessCheck"
+          }
+        }
+      }
+    },
+    "QuestionCandidate": {
+      "type": "object",
+      "additionalProperties": false,
+      "required": [
+        "id",
+        "question",
+        "origin",
+        "source_unit_ids",
+        "disposition",
+        "target_question_ids",
+        "reason",
+        "issue_ids"
+      ],
+      "properties": {
+        "id": {"$ref": "../common.schema.json#/$defs/Id"},
+        "question": {"$ref": "../common.schema.json#/$defs/Text"},
+        "origin": {"enum": ["SOURCE", "PROCESS"]},
+        "source_unit_ids": {"$ref": "../common.schema.json#/$defs/Ids"},
+        "disposition": {"enum": ["RETAINED", "MERGED", "OUT_OF_SCOPE", "OPEN"]},
+        "target_question_ids": {"$ref": "../common.schema.json#/$defs/Ids"},
+        "reason": {"$ref": "../common.schema.json#/$defs/Text"},
+        "issue_ids": {"$ref": "../common.schema.json#/$defs/Ids"}
+      },
+      "allOf": [
+        {
+          "if": {"properties": {"origin": {"const": "SOURCE"}}},
+          "then": {"properties": {"source_unit_ids": {"minItems": 1}}}
+        },
+        {
+          "if": {"properties": {"disposition": {"enum": ["RETAINED", "MERGED"]}}},
+          "then": {"properties": {"target_question_ids": {"minItems": 1}}}
+        },
+        {
+          "if": {"properties": {"disposition": {"enum": ["OUT_OF_SCOPE", "OPEN"]}}},
+          "then": {"properties": {"target_question_ids": {"maxItems": 0}}}
+        }
+      ]
+    },
+    "ProcessCheck": {
+      "type": "object",
+      "additionalProperties": false,
+      "required": [
+        "id",
+        "actor",
+        "stage",
+        "concern",
+        "source_unit_ids",
+        "question_ids",
+        "status",
+        "reason",
+        "issue_ids"
+      ],
+      "properties": {
+        "id": {"$ref": "../common.schema.json#/$defs/Id"},
+        "actor": {"$ref": "../common.schema.json#/$defs/Text"},
+        "stage": {"$ref": "../common.schema.json#/$defs/Text"},
+        "concern": {"$ref": "../common.schema.json#/$defs/Text"},
+        "source_unit_ids": {"$ref": "../common.schema.json#/$defs/Ids"},
+        "question_ids": {"$ref": "../common.schema.json#/$defs/Ids"},
+        "status": {"enum": ["COVERED", "GAP", "NOT_APPLICABLE"]},
+        "reason": {"$ref": "../common.schema.json#/$defs/Text"},
+        "issue_ids": {"$ref": "../common.schema.json#/$defs/Ids"}
+      }
+    },
     "CaseCoverage": {
       "type": "object",
       "additionalProperties": false,
--- old/contracts/input.schema.json
+++ new/contracts/input.schema.json
@@ -1,6 +1,6 @@
 {
   "$schema": "https://json-schema.org/draft/2020-12/schema",
-  "$id": "https://hovo.local/semantic-engineering/2/domain-model/input.schema.json",
+  "$id": "https://hovo.local/semantic-engineering/3/domain-model/input.schema.json",
   "allOf": [
     {
       "$ref": "../common.schema.json#/$defs/Input"
@@ -16,7 +16,9 @@
       "then": {
         "required": [
           "knowledge",
-          "review_mode"
+          "knowledge_basis",
+          "review_mode",
+          "question_scope_ids"
         ]
       }
     },
@@ -47,8 +49,47 @@
           "subjects",
           "change_request",
           "knowledge",
-          "review_mode"
+          "knowledge_basis",
+          "review_mode",
+          "question_scope_ids"
         ]
+      }
+    },
+    {
+      "if": {
+        "properties": {
+          "knowledge_basis": {
+            "const": "CONFIRMED"
+          }
+        },
+        "required": [
+          "knowledge_basis"
+        ]
+      },
+      "then": {
+        "required": [
+          "knowledge_confirmation",
+          "question_scope_ids"
+        ]
+      }
+    },
+    {
+      "if": {
+        "properties": {
+          "knowledge_basis": {
+            "const": "DRAFT"
+          }
+        },
+        "required": [
+          "knowledge_basis"
+        ]
+      },
+      "then": {
+        "not": {
+          "required": [
+            "knowledge_confirmation"
+          ]
+        }
       }
     }
   ],
@@ -58,9 +99,15 @@
       "$ref": "../common.schema.json#/$defs/ArtifactRef",
       "properties": {
         "contract_version": {
-          "const": "2.0.0"
+          "const": "4.0.0"
         }
       }
+    },
+    "knowledge_basis": {
+      "enum": [
+        "DRAFT",
+        "CONFIRMED"
+      ]
     },
     "knowledge_confirmation": {
       "$ref": "../common.schema.json#/$defs/EvidenceRef"
--- old/contracts/output.schema.json
+++ new/contracts/output.schema.json
@@ -1,6 +1,6 @@
 {
   "$schema": "https://json-schema.org/draft/2020-12/schema",
-  "$id": "https://hovo.local/semantic-engineering/2/domain-model/output.schema.json",
+  "$id": "https://hovo.local/semantic-engineering/3/domain-model/output.schema.json",
   "allOf": [
     {
       "$ref": "../common.schema.json#/$defs/Output"
@@ -35,7 +35,9 @@
               "case_explanations",
               "decisions",
               "knowledge_ref",
-              "question_scope_ids"
+              "question_scope_ids",
+              "knowledge_basis",
+              "upstream_issue_bindings"
             ],
             "properties": {
               "concepts": {
@@ -89,13 +91,73 @@
                 "$ref": "../common.schema.json#/$defs/ArtifactRef",
                 "properties": {
                   "contract_version": {
-                    "const": "2.0.0"
+                    "const": "4.0.0"
                   }
                 }
               },
               "question_scope_ids": {
                 "$ref": "../common.schema.json#/$defs/Ids",
                 "minItems": 1
+              },
+              "knowledge_basis": {
+                "enum": [
+                  "DRAFT",
+                  "CONFIRMED"
+                ]
+              },
+              "upstream_issue_bindings": {
+                "type": "array",
+                "items": {
+                  "$ref": "#/$defs/UpstreamIssueBinding"
+                }
+              }
+            },
+            "allOf": [
+              {
+                "if": {
+                  "properties": {
+                    "knowledge_basis": {
+                      "const": "DRAFT"
+                    }
+                  }
+                },
+                "then": {
+                  "properties": {
+                    "upstream_issue_bindings": {
+                      "minItems": 1
+                    }
+                  }
+                }
+              }
+            ]
+          }
+        }
+      }
+    },
+    {
+      "if": {
+        "properties": {
+          "content": {
+            "properties": {
+              "knowledge_basis": {
+                "const": "DRAFT"
+              }
+            },
+            "required": [
+              "knowledge_basis"
+            ]
+          }
+        },
+        "required": [
+          "content"
+        ]
+      },
+      "then": {
+        "properties": {
+          "confirmation": {
+            "properties": {
+              "status": {
+                "const": "PENDING"
               }
             }
           }
@@ -415,7 +477,9 @@
       "required": [
         "question_id",
         "model_ids",
-        "gap_ids"
+        "gap_ids",
+        "status",
+        "reason"
       ],
       "properties": {
         "question_id": {
@@ -426,24 +490,103 @@
         },
         "gap_ids": {
           "$ref": "../common.schema.json#/$defs/Ids"
+        },
+        "status": {
+          "enum": [
+            "MODELED",
+            "PARTIAL",
+            "DEFERRED"
+          ]
+        },
+        "reason": {
+          "$ref": "../common.schema.json#/$defs/Text"
         }
       },
-      "anyOf": [
+      "allOf": [
         {
-          "properties": {
-            "model_ids": {
-              "minItems": 1
+          "if": {
+            "properties": {
+              "status": {
+                "const": "MODELED"
+              }
+            }
+          },
+          "then": {
+            "properties": {
+              "model_ids": {
+                "minItems": 1
+              },
+              "gap_ids": {
+                "maxItems": 0
+              }
             }
           }
         },
         {
-          "properties": {
-            "gap_ids": {
-              "minItems": 1
+          "if": {
+            "properties": {
+              "status": {
+                "const": "PARTIAL"
+              }
+            }
+          },
+          "then": {
+            "properties": {
+              "model_ids": {
+                "minItems": 1
+              },
+              "gap_ids": {
+                "minItems": 1
+              }
+            }
+          }
+        },
+        {
+          "if": {
+            "properties": {
+              "status": {
+                "const": "DEFERRED"
+              }
+            }
+          },
+          "then": {
+            "properties": {
+              "gap_ids": {
+                "minItems": 1
+              }
             }
           }
         }
       ]
+    },
+    "UpstreamIssueBinding": {
+      "type": "object",
+      "additionalProperties": false,
+      "required": [
+        "knowledge_issue_id",
+        "model_issue_ids",
+        "handling",
+        "reason"
+      ],
+      "properties": {
+        "knowledge_issue_id": {
+          "$ref": "../common.schema.json#/$defs/Id"
+        },
+        "model_issue_ids": {
+          "$ref": "../common.schema.json#/$defs/Ids",
+          "minItems": 1
+        },
+        "handling": {
+          "enum": [
+            "MODEL_LIMITATION",
+            "DEFERRED",
+            "NOT_APPLICABLE"
+          ]
+        },
+        "reason": {
+          "$ref": "../common.schema.json#/$defs/Text"
+        }
+      }
     },
     "CaseExplanation": {
       "type": "object",
@@ -453,15 +596,15 @@
         "model_ids",
         "explanation",
         "expected",
-        "forbidden"
+        "forbidden",
+        "status"
       ],
       "properties": {
         "case_id": {
           "$ref": "../common.schema.json#/$defs/Id"
         },
         "model_ids": {
-          "$ref": "../common.schema.json#/$defs/Ids",
-          "minItems": 1
+          "$ref": "../common.schema.json#/$defs/Ids"
         },
         "explanation": {
           "$ref": "../common.schema.json#/$defs/Text"
@@ -471,8 +614,32 @@
         },
         "forbidden": {
           "$ref": "../common.schema.json#/$defs/Text"
-        }
-      }
+        },
+        "status": {
+          "enum": [
+            "EXPLAINED",
+            "BLOCKED"
+          ]
+        }
+      },
+      "allOf": [
+        {
+          "if": {
+            "properties": {
+              "status": {
+                "const": "EXPLAINED"
+              }
+            }
+          },
+          "then": {
+            "properties": {
+              "model_ids": {
+                "minItems": 1
+              }
+            }
+          }
+        }
+      ]
     },
     "Decision": {
       "type": "object",
--- old/manifest.json
+++ new/manifest.json
@@ -1,8 +1,8 @@
 {
   "name": "domain-model",
-  "version": "0.3.0",
+  "version": "0.5.0",
   "owner": "Hovo",
-  "updated_at": "2026-09-10",
+  "updated_at": "2026-09-16",
   "status": "candidate",
   "maturity_tier": "scaffold",
   "target_maturity": "production",
@@ -25,11 +25,16 @@
     "separate_verification_states",
     "typed_business_references",
     "problem_and_case_coverage",
-    "mode_specific_input_and_output"
+    "mode_specific_input_and_output",
+    "explicit_knowledge_basis",
+    "draft_issue_binding_and_no_formal_handoff",
+    "confirmed_scope_record",
+    "separate_business_review_and_coverage"
   ],
   "skill_dependencies": [],
   "bundled_resources": {
-    "contract": "2.0.0",
+    "contract": "3.0.0",
+    "domain_knowledge_input_contract": "4.0.0",
     "handbooks": "2.1"
   },
   "contract_validator": "scripts/validate_contract.py"
--- old/references/acceptance.md
+++ new/references/acceptance.md
@@ -1,29 +1,29 @@
 # 领域模型验收
 
-建模方法见 [六项检查](ontology-engineering-method.md) 与 [模式](modeling-patterns.md)，业务呈现见 [模型审查示例](review-guide.md)。只使用与当前业务问题相关的方法章节。
+本清单验收领域模型阶段的业务表达和交接边界，不验收数据映射、平台资产、发布、运行或业务结论。
 
-## 输入与模式
+## 准入
 
-创建或修订接收确认知识、业务案例和规范模型。REVIEW 只需已有模型即可开展，缺少知识确认列为审查限制；现有模型含字段或平台术语时可指出污染，不以它们补造业务定义。
+- PRODUCE 或 REVISE 已选择 DRAFT 或 CONFIRMED，且该选择独立于 IN_SESSION 或 DEFERRED。
+- 上游引用是固定的领域知识 4.0.0 非 REVIEW 成果，路径、版本和 SHA-256 摘要可核验。
+- CONFIRMED 有真实外部 ConfirmationRecord，精确绑定知识字节、答复文件与本次问题范围。DRAFT 没有该记录，也没有把知识的 PENDING scope 当作已确认范围。
 
-创建模型的上下文不含物理表、字段、SQL、算子或运行配置。来源可保留定位和字节摘要，模型生成不追读源数据结构。宿主上下文隔离能力不足时说明限制；Markdown 和 Schema 不构成文件访问沙箱。
+## 模型质量
 
-## 业务结构
+- 概念、角色、属性、关系和机理独立于表、字段、SQL、平台算子和运行配置；各有业务依据、正例和反例。
+- 身份、时间、状态、事件、度量、证据和未知语义绑定具体要素，写明条件、行为、冲突、缺证和禁止结果。
+- 每个判断机理可用业务语言复核输入、规则、操作、输出和缺证行为，不把模型写成平台脚本。
+- 范围内每个问题都有 question_coverage，案例预期和禁止结果逐字继承知识成果。
 
-- 每个概念说明定义、粒度、身份、生命周期、依据、正例及反例，区分实体、角色、事实、观察、证据、推导结果和任务。分类不强制固定层数，多维特征不误作互斥永久类型。
-- 属性定义业务含义、归属、值类型、单位、合法值、未知与系统更换后的稳定性；cardinality 明确 min/max，max=null 表示无上界。关系分别说明两个方向的基数与有效范围。
-- 身份、时间、状态、事件、度量、证据、未知分别标明适用性。适用政策必须通过 applies_to_ids 绑定具体概念、属性或关系，并有 basis_ids、前提、条件与行为、未知、冲突和禁止结果。不适用写业务理由。
-- 身份政策审查更名、重复证据、换人、合并与拆分；时间政策审查有效时点、观察时点、记录时间、知识截止及未知终止。状态与事件政策审查触发、前后变化和历史更正。
-- 证据政策说明何时采用、何时排除、冲突和重复如何处理；直接事实与计算结果分开，不凭最新值、空值或缺少证据擅自形成正式判断。
-- 每项机理列规则、输入输出要素和适用政策，input_ids 明确给定事实或属性，steps 有明确顺序，每步列输入、条件、业务操作、输出、缺证行为；不能读取尚未提供或产生的中间量，APPLY_RULE 必须引用具体规则。中间业务量先定义为业务属性或事实，再引用。
-- 关系对象化必须解释独立身份、时间、参与者与引用需要，不为存储习惯增加空壳节点，也不为假定性能删除真实业务事实。
+## 待审项与交付
 
-## 覆盖与确认
+- DRAFT 对每个开放上游 issue 都有 upstream_issue_bindings；对应模型 issue 为 OPEN，并明确影响与处理方式。
+- MODELED 没有开放模型缺口。PARTIAL、DEFERRED 和 BLOCKED 说明原因，并留下可定位的 MODEL_GAP；不得用 PASS、完成或默认解释掩盖。
+- review.md 是业务审查材料，coverage.md 是审计表；两者分别存在、各有正确摘要，并与 output.json 使用相同 content_version。
+- 会话和线下反馈都绑定固定版本和范围。实质修订产生新版本，受影响范围重新评审。
 
-knowledge_ref 固定知识版本并列入 input_refs。question_scope_ids、模型确认范围与 question_coverage 一致；每个范围内问题有模型落点或 MODEL_GAP。引用指向正确类别，继承不循环，知识到模型的命名保持可区分。
+## 证据边界
 
-case_explanations 覆盖范围内已有案例，逐字保留原 expected 与 forbidden；解释说明模型如何得到该结果。发现原预期有误，提出知识变更请求。缺案例保留知识缺口，不造出已验收结果。
+结构检查通过只证明合同形状、精确引用和有限覆盖。只有真实责任人对固定范围的外部 ConfirmationRecord 才能证明确认；它仍不证明来源真实性、业务逻辑正确、平台状态或实际控制人结论。
 
-review.md 按“问题与范围、概念及关系、业务属性、身份及变化、事实与证据采用、判断步骤、案例、重大取舍与待确认事项”组织。概念关系可用简图，属性用清晰表格。完整性不以属性连线数量衡量。
-
-只有责任人对固定事项和版本的真实答复才是确认。结构与引用通过不证明概念正确、机理正确或事实可采信。REVIEW 输出审查报告即可完成，不能因此声明已生成或批准新模型。
+创建和修订必须明确非空问题范围，且属于知识问题目录与知识声明范围；声明范围本身不是批准。DRAFT和CONFIRMED都逐项处置上游开放事项，范围外事项可注明不适用及原因，不能遗漏已影响本次问题的缺口。每题覆盖表须列全实际影响该题的开放模型缺口。知识的正式确认记录同时绑定知识output、review.md、coverage.md及真实答复；已REJECTED或STALE的基线不能借旧批准绕过。
--- old/references/interactive-review.md
+++ new/references/interactive-review.md
@@ -1,19 +1,19 @@
-# 交互式评审
+# 模型评审与线下回路
 
-领域模型不是在“文件写完”后才开始评审。本技能在形成可读模型审查稿后，必须选择本轮评审方式，并在交付中写明选择。
+review_mode 只管理模型的评审组织方式；knowledge_basis 管理模型采用的知识依据。这两个选择独立。
 
-## IN_SESSION
+## 会话评审
 
-适用于当前对话中已有能够代表业务、合规或模型责任的答复者。先用业务语言展示：本次范围、关键概念及关系、重大取舍、未决事项、案例的预期与禁止结果。一次只问能作出实际选择的问题；每项答复记录答复者、角色、时间、范围、原话或忠实摘要，以及对概念、案例和机理的影响。
+IN_SESSION 时，先展示 review.md 的范围、概念与关系、判断步骤、案例、上游未决项和模型分歧。按问题逐项记录答复者、角色、时间、答复内容、影响的模型要素和案例。答复文件作为独立 ArtifactRef 保存，不回写已经确认的字节。
 
-答复覆盖的事项可形成外部 `ConfirmationRecord` 并绑定固定知识/模型字节。答复不覆盖的事项继续保持待确认；模型范围必须缩小到已批准问题，不能用一次“整体同意”覆盖未展示的分歧。
+如果知识依据为 DRAFT，会话可评议模型，但模型仍保持 PENDING，不能以本轮讨论制造上游知识确认。知识依据为 CONFIRMED 时，仍应把模型本身的确认范围与上游知识确认分开记录。
 
-## DEFERRED
+## 线下评审
 
-仅在用户明确选择本轮跳过评审、或当前会话没有可作答责任人时使用。输出标为“待审草案”，列明未审范围、恢复评审时应答复的问题和所需材料。草案可完成当前整理工作，但不得作为需要已确认知识的正式下游交接输入。
+DEFERRED 时交付待审 review.md、coverage.md、output.json 和原始反馈入口。业务人员只需按可读标题对范围、概念、条件、例外、案例、证据和未决项给出意见；不要求编辑 JSON 或内部 ID。
 
-后续恢复时，读取固定草案及未决项，按 `IN_SESSION` 收集答复；发生实质修改时重新固定版本，旧答复不自动覆盖新字节。
+收回反馈后，先核对其绑定的版本、摘要和范围，再执行 REVISE：保存原反馈，列受影响问题、案例、概念和机制，生成新版本，并将受影响旧确认标为历史。新的范围重新进入会话或线下评审。
 
-## 不可替代的动作
+## 禁止替代
 
-作者自查、脚本通过、模型建议和用户未答复都不是业务确认。不得把 `DEFERRED` 写成失败，也不得把它写成批准；它只说明评审被有意延后。
+作者自查、自动检查、模型建议、会议纪要未答复、PENDING scope 和结构 PASS 都不是确认。DRAFT 可完成草案整理，不能正式交接；CONFIRMED 需要真实的外部 ConfirmationRecord，而不是生成器内联字段。
--- old/references/sources.json
+++ new/references/sources.json
@@ -1,42 +1,57 @@
 {
   "skill_id": "domain-model",
-  "skill_version": "0.3.0",
-  "recorded_at": "2026-09-10",
-  "origins": {
-    "handbook": "User-provided Hovo financial semantic engineering handbooks V2.1, volumes 00-02, unchanged bytes",
-    "ecp-kit": "User-provided ECP Semantic Authoring Kit 1.7.0, unchanged bytes; original manifest retained",
-    "asset-contract": "Hovo fixed stage and input contracts, with an explicit version per resource",
-    "local-tool": "Hovo local validation and packaging tools, bundled for this skill"
-  },
-  "runtime_resource_policy": "All listed paths are relative to this skill; no loading from other skill directories or automatic resource downloads.",
+  "skill_version": "0.5.0",
+  "recorded_at": "2026-09-16",
+  "runtime_resource_policy": "All listed paths are relative to this skill. The bundled knowledge schemas validate input shape only; this package does not load another skill directory or download resources.",
   "files": [
     {
       "path": "contracts/common.schema.json",
       "category": "asset-contract",
-      "version": "2.0.0",
+      "version": "3.0.0",
       "byte_length": 12983,
-      "digest": "sha256:f5f36c389eef83ab4bb42fe31af3c54a2176c68df57ba20f42b0bf06ad13b97e"
-    },
-    {
-      "path": "contracts/imports/domain-knowledge.schema.json",
-      "category": "asset-contract",
-      "version": "2.0.0",
-      "byte_length": 11255,
-      "digest": "sha256:9b60dbefbde5f772513ba207baf1a5066302d77cbec6300e63a29599df8930fa"
+      "digest": "sha256:b4d258e6344c3827c2b629546eb121a7ec54c2fc75ce5ce28e4f7f78bd99a958"
     },
     {
       "path": "contracts/input.schema.json",
       "category": "asset-contract",
-      "version": "2.0.0",
-      "byte_length": 1759,
-      "digest": "sha256:e109a6562d6fa184de84dd9b1aa6e76e7046ea5a9843782b43ca88dfa3b194b2"
+      "version": "3.0.0",
+      "byte_length": 2702,
+      "digest": "sha256:d0c2ead496b99e907cb184cfba381320eb5e1a09e1a90f976364965278dcfdb3"
     },
     {
       "path": "contracts/output.schema.json",
       "category": "asset-contract",
-      "version": "2.0.0",
-      "byte_length": 16768,
-      "digest": "sha256:0a593a93ce1159afea3d2bd7f96b712ebe1e87b0bd36ff5c76349891faae3843"
+      "version": "3.0.0",
+      "byte_length": 20492,
+      "digest": "sha256:f360eec8114345a237b3d997d2f7d5163464c6d5c5d3edc96e7f1a229957259f"
+    },
+    {
+      "path": "contracts/imports/domain-knowledge.schema.json",
+      "category": "upstream-contract-snapshot",
+      "version": "4.0.0",
+      "byte_length": 17440,
+      "digest": "sha256:bedefc9b9d62e98a4cdc2d9d5bb1439c47f069f0b61ee5bc65ae48d624f94603"
+    },
+    {
+      "path": "contracts/imports/domain-knowledge-common.schema.json",
+      "category": "upstream-contract-snapshot",
+      "version": "4.0.0",
+      "byte_length": 13423,
+      "digest": "sha256:db5706ae7903223a65a792f33eacd830a39db4e565faaa6aa2586e6d087e5c35"
+    },
+    {
+      "path": "scripts/domain_checks.py",
+      "category": "local-tool",
+      "version": "0.5.0",
+      "byte_length": 15689,
+      "digest": "sha256:02a66c17a0d64b7b43af68c29fe7e45333602449877d4bad47f3a7db400a4645"
+    },
+    {
+      "path": "scripts/validate_contract.py",
+      "category": "local-tool",
+      "version": "0.5.0",
+      "byte_length": 20028,
+      "digest": "sha256:d5c19397fc4f697c71ca3a57cfabe7126c855209898d56b052f4827def49b3da"
     },
     {
       "path": "references/handbooks/00_金融领域面向业务决策的语义工程方法论_V2.1.md",
@@ -58,20 +73,6 @@
       "version": "2.1",
       "byte_length": 30145,
       "digest": "sha256:76ffba263388476039b162e2d3881104c89f0db36238a51019b1be5b1cac0b72"
-    },
-    {
-      "path": "scripts/domain_checks.py",
-      "category": "local-tool",
-      "version": "0.3.0",
-      "byte_length": 11698,
-      "digest": "sha256:d8f46fe814acbd5f6df738c21e969bb168c7be1c4a89462626a803757822e378"
-    },
-    {
-      "path": "scripts/validate_contract.py",
-      "category": "local-tool",
-      "version": "0.3.0",
-      "byte_length": 16397,
-      "digest": "sha256:a9ae8b46e48b4cb37d7770fe8c8ab05662c724fd24f3178a52293f19374726ac"
     }
   ]
 }
--- old/scripts/domain_checks.py
+++ new/scripts/domain_checks.py
@@ -107,8 +107,10 @@
         kc = knowledge["content"]
         statements, terms = index(kc["statements"]), index(kc["terms"])
         rules, questions, cases = index(kc["rules"]), index(kc["questions"]), index(kc["cases"])
+        knowledge_issues = index(knowledge["issues"])
         knowledge_ids = statements.keys() | terms.keys() | rules.keys() | questions.keys() | cases.keys()
         allowed |= knowledge_ids
+        allowed |= knowledge_issues.keys()
         allowed |= {s["source_id"] for s in kc["sources"]}
         concepts = index(content["concepts"])
         attributes = index(content["attributes"])
@@ -125,11 +127,44 @@
             fail("ID_NAMESPACE_COLLISION", collision, "模型新标识不能与知识标识重复；请使用明确前缀")
         scope = set(content["question_scope_ids"])
         refs(scope, questions, "question_scope_ids")
-        refs(scope, set(knowledge["confirmation"]["scope_ids"]), "knowledge/confirmation_scope")
+        refs(scope, set(knowledge["confirmation"]["scope_ids"]), "knowledge/declared_scope")
         if set(payload["confirmation"]["scope_ids"]) != scope:
             fail("CONFIRMATION_SCOPE_MISMATCH", "confirmation/scope_ids", "模型确认范围必须与能力问题范围一致")
+        if content["knowledge_basis"] == "DRAFT" and payload["confirmation"]["status"] != "PENDING":
+            fail("DRAFT_FORMAL_CONFIRMATION_FORBIDDEN", "confirmation/status", "DRAFT 模型必须保持 PENDING，不能正式交接或确认")
         if content["knowledge_ref"] not in payload["input_refs"]:
             fail("KNOWLEDGE_REF_UNBOUND", "content/knowledge_ref", "知识基线必须登记为精确输入引用")
+        bindings = content["upstream_issue_bindings"]
+        open_knowledge_issues = {
+            issue_id for issue_id, issue in knowledge_issues.items() if issue["status"] == "OPEN"
+        }
+        exact_coverage(
+            [row["knowledge_issue_id"] for row in bindings],
+            open_knowledge_issues,
+            "upstream_issue_bindings",
+        )
+        for row in bindings:
+            refs([row["knowledge_issue_id"]], knowledge_issues, "upstream_issue_bindings/knowledge_issue_id")
+            refs(row["model_issue_ids"], issues, "upstream_issue_bindings/model_issue_ids")
+            for model_issue_id in row["model_issue_ids"]:
+                model_issue = issues.get(model_issue_id)
+                if model_issue and model_issue["kind"] != "MODEL_GAP":
+                    fail("UPSTREAM_ISSUE_KIND_INVALID", model_issue_id, "上游未决项必须映射为 MODEL_GAP")
+                if model_issue and model_issue["status"] != "OPEN":
+                    fail("UPSTREAM_ISSUE_STATUS_INVALID", model_issue_id, "上游未决项未关闭前模型问题必须保持 OPEN")
+                if model_issue and row["knowledge_issue_id"] not in model_issue["affects"]:
+                    fail("UPSTREAM_ISSUE_IMPACT_MISSING", model_issue_id, "模型问题必须在 affects 中登记上游问题 ID")
+            upstream = knowledge_issues.get(row["knowledge_issue_id"], {})
+            affected_questions = set(upstream.get("affects", [])) & scope
+            bound_questions = {
+                target
+                for issue_id in row["model_issue_ids"]
+                for target in issues.get(issue_id, {}).get("affects", [])
+            } & scope
+            if not affected_questions.issubset(bound_questions):
+                fail("UPSTREAM_QUESTION_IMPACT_MISSING", row["knowledge_issue_id"], "模型问题没有承接上游问题对本次问题范围的影响")
+            if row["handling"] == "NOT_APPLICABLE" and affected_questions:
+                fail("UPSTREAM_NOT_APPLICABLE_INVALID", row["knowledge_issue_id"], "上游问题已影响当前范围，不能标不适用")
         basis_ids = statements.keys() | terms.keys() | rules.keys()
         for item in (*concepts.values(), *attributes.values(), *relationships.values(), *policies.values()):
             refs(item["basis_ids"], basis_ids, item["id"] + "/basis_ids")
@@ -166,11 +201,20 @@
                 produced.update(step["output_ids"])
             refs(item["output_ids"], produced, item["id"] + "/produced_outputs")
         exact_coverage([row["question_id"] for row in content["question_coverage"]], scope, "question_coverage")
+        coverage_by_question = {row["question_id"]: row for row in content["question_coverage"]}
         for row in content["question_coverage"]:
             refs(row["model_ids"], model_ids, "question_coverage/model_ids")
             gaps = {key for key, item in issues.items() if item["kind"] == "MODEL_GAP" and item["status"] == "OPEN" and row["question_id"] in item["affects"]}
             refs(row["gap_ids"], gaps, "question_coverage/gap_ids")
+            if set(row["gap_ids"]) != gaps:
+                fail("QUESTION_GAPS_INCOMPLETE", row["question_id"], "问题覆盖必须列出所有影响该问题的开放模型缺口")
             linked = {key for key, item in mechanisms.items() if row["question_id"] in item["question_ids"]}
+            if row["status"] == "MODELED" and row["gap_ids"]:
+                fail("MODELED_WITH_GAP", row["question_id"], "存在开放模型问题时不能标记为 MODELED")
+            if row["status"] == "PARTIAL" and not row["gap_ids"]:
+                fail("PARTIAL_WITHOUT_GAP", row["question_id"], "PARTIAL 必须关联开放模型问题")
+            if row["status"] == "DEFERRED" and not row["gap_ids"]:
+                fail("DEFERRED_WITHOUT_GAP", row["question_id"], "DEFERRED 必须关联开放模型问题")
             if linked and not linked.intersection(row["model_ids"]) and not row["gap_ids"]:
                 fail("MECHANISM_COVERAGE_MISSING", row["question_id"], "能力问题没有关联对应判断机理")
         expected_cases = {key for key, item in cases.items() if scope.intersection(item["question_ids"])}
@@ -180,6 +224,18 @@
             case = cases.get(item["case_id"])
             if case and (item["expected"] != case["expected"] or item["forbidden"] != case["forbidden"]):
                 fail("CASE_EXPECTATION_CHANGED", item["case_id"], "模型不能改写知识基线的预期或禁止结果")
+            if item["status"] == "EXPLAINED" and not item["model_ids"]:
+                fail("CASE_EXPLANATION_MISSING", item["case_id"], "EXPLAINED 案例必须关联模型要素")
+            if item["status"] == "BLOCKED":
+                related_coverage = [
+                    coverage_by_question.get(question_id)
+                    for question_id in (case or {}).get("question_ids", [])
+                ]
+                if not any(
+                    row and row["status"] in {"PARTIAL", "DEFERRED"} and row["gap_ids"]
+                    for row in related_coverage
+                ):
+                    fail("CASE_BLOCKED_UNJUSTIFIED", item["case_id"], "BLOCKED 案例必须由关联问题的开放模型问题说明")
     for issue in issues.values():
         refs(issue["affects"], allowed, issue["id"] + "/affects")
     for item in payload["trace"]:
--- old/scripts/validate_contract.py
+++ new/scripts/validate_contract.py
@@ -226,52 +226,96 @@
     return references
 
 
+def check_model_documents(payload, failures):
+    """Keep business review material separate from the audit coverage table."""
+    if payload["mode"] == "REVIEW":
+        return
+    by_name = defaultdict(list)
+    for reference in payload["files"]:
+        by_name[PurePosixPath(reference["path"]).name].append(reference)
+    for name in ("review.md", "coverage.md"):
+        matches = by_name[name]
+        if len(matches) != 1:
+            failure(failures, "REQUIRED_DOCUMENT_INVALID", "files", f"必须且只能登记一份 {name}")
+            continue
+        if matches[0]["content_version"] != payload["content_version"]:
+            failure(failures, "DOCUMENT_VERSION_MISMATCH", name, f"{name} 必须使用当前 content_version")
+
+
+def load_knowledge_baseline(reference, project_root, schemas, registry, failures):
+    """Load exactly the bundled 4.0.0 knowledge artifact; never borrow another skill's schema."""
+    try:
+        baseline = load_json(canonical_artifact_path(project_root, reference["path"]), root=project_root)
+        validator = Draft202012Validator(
+            schemas["domain-knowledge:output"], registry=registry, format_checker=FormatChecker())
+        errors = list(validator.iter_errors(baseline))
+        if errors:
+            raise ValueError("知识基线合同错误: " + errors[0].message)
+        if baseline["mode"] == "REVIEW":
+            raise ValueError("审查报告不能代替知识基线")
+        for key in ("artifact_id", "content_version", "contract_version"):
+            if reference[key] != baseline[key]:
+                raise ValueError("知识引用身份或版本不匹配: " + key)
+        check_artifact_refs(baseline, project_root, failures)
+        return baseline
+    except (KeyError, TypeError, OSError, ValueError) as exc:
+        failure(failures, "KNOWLEDGE_BASELINE_INVALID", "knowledge", str(exc))
+        return None
+
+
+def check_confirmation_evidence(reference, scope, evidence, project_root, schemas, registry, failures, location):
+    """Accept only a real external ConfirmationRecord bound to this exact knowledge bytes and scope."""
+    try:
+        record = load_json(canonical_artifact_path(project_root, evidence["record"]["path"]), root=project_root)
+        validator = Draft202012Validator(
+            {"$ref": schemas["common"]["$id"] + "#/$defs/ConfirmationRecord"},
+            registry=registry, format_checker=FormatChecker())
+        errors = list(validator.iter_errors(record))
+        if errors:
+            raise ValueError("确认记录结构错误: " + errors[0].message)
+        check_artifact_refs(record, project_root, failures)
+        if record["status"] != "APPROVED":
+            raise ValueError("前序基线未获批准")
+        if reference not in evidence["subject_refs"] or reference not in record["subjects"]:
+            raise ValueError("确认记录没有绑定此次输入的完整身份、版本和字节摘要")
+        baseline = load_json(canonical_artifact_path(project_root, reference["path"]), root=project_root)
+        if baseline.get("confirmation", {}).get("status") in {"REJECTED", "STALE"}:
+            raise ValueError("知识基线已拒绝或失效，不能使用旧批准记录")
+        declared = set(baseline.get("confirmation", {}).get("scope_ids", []))
+        if not set(scope) or not set(scope).issubset(declared):
+            raise ValueError("确认范围为空或超出知识声明范围")
+        documents = {PurePosixPath(item["path"]).name: item for item in baseline["files"]}
+        for name in ("review.md", "coverage.md"):
+            document = documents.get(name)
+            if document is None or document not in record["subjects"]:
+                raise ValueError("确认记录必须同时绑定知识评审材料: " + name)
+        if not set(scope).issubset(record["scope_ids"]):
+            raise ValueError("确认记录没有覆盖该成果的交接范围")
+        return True
+    except (KeyError, TypeError, OSError, ValueError) as exc:
+        failure(failures, "CONFIRMATION_INVALID", location, str(exc))
+        return False
+
+
 def check_confirmations(payload, project_root, schemas, registry, failures):
-    pairs = (("knowledge", "knowledge_confirmation", "domain-knowledge"),)
-    count = 0
-    for target_key, confirmation_key, stage in pairs:
-        if confirmation_key not in payload:
-            if payload.get("mode") in {"PRODUCE", "REVISE"} and payload.get("review_mode") == "IN_SESSION":
-                failure(failures, "CONFIRMATION_REQUIRED", confirmation_key, "IN_SESSION 必须提供已批准的知识确认记录")
-            continue
-        count += 1
-        try:
-            reference = payload[target_key]
-            evidence = payload[confirmation_key]
-            record = load_json(canonical_artifact_path(project_root, evidence["record"]["path"]), root=project_root)
-            validator = Draft202012Validator(
-                {"$ref": schemas["common"]["$id"] + "#/$defs/ConfirmationRecord"},
-                registry=registry, format_checker=FormatChecker())
-            errors = list(validator.iter_errors(record))
-            if errors:
-                raise ValueError("确认记录结构错误: " + errors[0].message)
-            check_artifact_refs(record, project_root, failures)
-            if record["status"] != "APPROVED":
-                raise ValueError("前序基线未获批准")
-            if reference not in evidence["subject_refs"] or reference not in record["subjects"]:
-                raise ValueError("确认记录没有绑定此次输入的完整身份、版本和字节摘要")
-            baseline = load_json(canonical_artifact_path(project_root, reference["path"]), root=project_root)
-            validator = Draft202012Validator(schemas[f"{stage}:output"], registry=registry, format_checker=FormatChecker())
-            errors = list(validator.iter_errors(baseline))
-            if errors:
-                raise ValueError("被确认成果不满足知识合同: " + errors[0].message)
-            if baseline["mode"] == "REVIEW":
-                raise ValueError("审查报告不能代替知识基线")
-            check_content(baseline, failures)
-            for key in ("artifact_id", "content_version", "contract_version"):
-                if reference[key] != baseline[key]:
-                    raise ValueError("被确认成果与引用的 " + key + " 不一致")
-            scope = set(payload.get("question_scope_ids", baseline["confirmation"]["scope_ids"]))
-            if not scope.issubset(baseline["confirmation"]["scope_ids"]):
-                raise ValueError("请求超出知识基线的交接范围")
-            if not scope.issubset(record["scope_ids"]):
-                raise ValueError("确认记录没有覆盖该成果的交接范围")
-            check_artifact_refs(baseline, project_root, failures)
-            check_ids(baseline, failures)
-            check_evidence_references(baseline, failures)
-        except (KeyError, TypeError, OSError, ValueError) as exc:
-            failure(failures, "CONFIRMATION_INVALID", confirmation_key, str(exc))
-    return count
+    """knowledge_basis and review_mode are deliberately independent."""
+    if payload["mode"] == "REVIEW":
+        return 0
+    baseline = load_knowledge_baseline(payload["knowledge"], project_root, schemas, registry, failures)
+    if baseline is None:
+        return 0
+    scope = set(payload.get("question_scope_ids", []))
+    questions = {item["id"] for item in baseline["content"]["questions"]}
+    declared_scope = set(baseline.get("confirmation", {}).get("scope_ids", []))
+    if not scope or not scope.issubset(questions) or not scope.issubset(declared_scope):
+        failure(failures, "QUESTION_SCOPE_INVALID", "question_scope_ids", "请求范围为空或超出知识问题与声明范围；声明范围不等于已批准范围")
+    if payload["knowledge_basis"] == "DRAFT":
+        if "knowledge_confirmation" in payload:
+            failure(failures, "DRAFT_CONFIRMATION_FORBIDDEN", "knowledge_confirmation", "DRAFT 不得携带确认记录")
+        return 0
+    return int(check_confirmation_evidence(
+        payload["knowledge"], scope, payload["knowledge_confirmation"],
+        project_root, schemas, registry, failures, "knowledge_confirmation"))
 
 
 def check_handoff(direction: str, file: Path, project_root: Path) -> dict[str, Any]:
@@ -298,22 +342,42 @@
             knowledge = None
             if stage == "domain-model" and payload["mode"] != "REVIEW":
                 reference = payload["content"]["knowledge_ref"]
-                knowledge = load_json(canonical_artifact_path(project_root, reference["path"]), root=project_root)
-                validator = Draft202012Validator(schemas["domain-knowledge:output"], registry=registry, format_checker=FormatChecker())
-                errors = list(validator.iter_errors(knowledge))
-                if errors:
-                    raise ValueError("知识基线合同错误: " + errors[0].message)
-                if knowledge["mode"] == "REVIEW":
-                    raise ValueError("审查报告不能代替知识基线")
-                for key in ("artifact_id", "content_version", "contract_version"):
-                    if reference[key] != knowledge[key]:
-                        raise ValueError("知识引用身份或版本不匹配: " + key)
-                check_artifact_refs(knowledge, project_root, failures)
-                check_ids(knowledge, failures)
-                check_evidence_references(knowledge, failures)
-                check_content(knowledge, failures)
+                knowledge = load_knowledge_baseline(reference, project_root, schemas, registry, failures)
+                if knowledge is not None and payload["content"]["knowledge_basis"] == "CONFIRMED":
+                    scope = payload["content"]["question_scope_ids"]
+                    candidates = [
+                        evidence for evidence in payload["evidence"]
+                        if reference in evidence["subject_refs"]
+                    ]
+                    if not candidates:
+                        failure(
+                            failures,
+                            "CONFIRMATION_REQUIRED",
+                            "evidence",
+                            "CONFIRMED 必须登记绑定知识基线的外部 ConfirmationRecord",
+                        )
+                    else:
+                        # Non-confirmation evidence may also mention this knowledge artifact.
+                        # Reject the claim only if no candidate is a valid approval record.
+                        candidate_errors = []
+                        valid_confirmation = False
+                        for index, evidence in enumerate(candidates):
+                            local_failures = []
+                            valid = check_confirmation_evidence(
+                                reference, scope, evidence, project_root, schemas, registry,
+                                local_failures, f"evidence/{index}")
+                            if valid and not local_failures:
+                                valid_confirmation = True
+                                break
+                            candidate_errors.extend(local_failures)
+                        if not valid_confirmation:
+                            failures.extend(candidate_errors)
+                            failure(failures, "CONFIRMATION_INVALID", "evidence",
+                                    "没有可用于本知识范围的已批准 ConfirmationRecord")
             if not failures:
                 check_content(payload, failures, knowledge)
+                if not failures and stage == "domain-model":
+                    check_model_documents(payload, failures)
                 checked["businessReferences"] = "CHECKED"
                 checked["coverage"] = "CHECKED" if payload["mode"] != "REVIEW" else "NOT_APPLICABLE"
     except (OSError, UnicodeError, json.JSONDecodeError, ValueError, exceptions.SchemaError, Unresolvable) as exc:
