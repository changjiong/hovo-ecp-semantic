# 过程与评审记录

本目录保存 A01 领域知识与领域模型的输入请求、任务跟踪、评审答复、人员确认、验证快照、历史版本和旧打包件。这些文件用于追溯，不是当前最终交付入口。

当前最终产物只在：

- `../03领域知识输出/`：`review.md`、`coverage.md`、`output.json`
- `../04领域模型输出/`：`model.yaml`、`review.md`、`coverage.md`、`output.json`
- `../dist/`：按上述白名单组装的当前最终 ZIP

`validation*.json`、旧版 README 和历史审计文件记录的是当时的路径与执行结果；迁移后不改写其历史内容，也不把它们当作当前版本的重新验证证据。
