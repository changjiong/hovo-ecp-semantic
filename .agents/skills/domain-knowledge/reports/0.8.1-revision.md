# 0.8.1 修订说明（2026-09-18）

- 合同：`output.schema.json` 的 `Rule` 增加可选字段 `name`（规则业务名称），不改变既有必填项，向后兼容；合同版本仍为 4.0.0。
- 渲染：`render_documents.py` 的规则标题改为「规则 NN｜<业务名称>（K.R.xx）」，未提供名称时回退原模板；「问题与规则编号索引」增加规则名称列。
- 背景：此前 59 条规则标题均为“判断依据与步骤”，无法检索；A01 受益所有人项目据此升版为 2.5.0。
