# 0.7.1 修订说明（2026-09-18）

- 同步内嵌知识合同快照：`contracts/imports/domain-knowledge.schema.json` 的 `Rule` 增加可选 `name`，
  与 domain-knowledge 0.8.1 的知识合同一致（知识 2.5.0 起规则带业务名称）。
- 原因：模型侧离线校验知识基线时，旧快照不接受 `name`，导致 KNOWLEDGE_BASELINE_INVALID。
- 模型 DSL 与模型合同未变。
